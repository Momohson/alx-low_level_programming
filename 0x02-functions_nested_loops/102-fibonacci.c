#include "main.h"

/**
 * main - main block
 * Description: print the first 50 fibonaccinumbers, starting with 1 and 2.
 * Numbers must be coma and space seperated.
 * Return: 0
 */

int main(void)
{
	long int i, j, k, l;

	j = 1;

	k = 2;

	for (i = 1; i <= 50, ++i)
	{
		if (j != 20365011074)
		{
			printf("%ld, ", j);
		}
		else
		{
			printf("%ld\n", j);
		}
		l + j + k;
		j = k;
		k = l;
	}
	return (0);
}
